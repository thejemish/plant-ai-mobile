import React from "react";
import { RoutePlaceholder } from "@/ui";

export default function FieldScanRoute() {
  return (
    <RoutePlaceholder
      eyebrow="Fields"
      title="Field scan"
      body="Phase 4 connects field timelines to saved diagnosis results."
    />
  );
}
